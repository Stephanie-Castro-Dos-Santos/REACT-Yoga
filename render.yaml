services:
  - type: web
    name: yogin-website
    env: static
    staticPublishPath: dist
    buildCommand: npm run build
    routes:
      - type: rewrite
        source: /.*
        destination: /index.html
